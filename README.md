# CI_Project
This repository contains a from-scratch neural network library implemented in Python using only NumPy. The library supports dense layers, activation functions (Sigmoid, Tanh, ReLU, Softmax), Mean Squared Error loss, and Stochastic Gradient Descent (SGD) optimizer.
